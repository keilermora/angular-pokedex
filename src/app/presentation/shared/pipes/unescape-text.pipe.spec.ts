import { UnescapeTextPipe } from './unescape-text.pipe';

describe('UnescapeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new UnescapeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
